package com.example.oneclickdrive.helper

interface CalculateListener {

    fun getIntersectNumbers(intersectNumbers: String)

    fun getUnionOfNumbers(unionOfNumbers: String)

    fun getLargestNumber(largestNumber: String)
}